To use any of these example on your project, use the `CMakeLists.txt` from the [`template/`](../template/).

These examples can be built all together as a single cmake project,
from the `example/build/` directory.
